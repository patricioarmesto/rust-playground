const PI: f32 = 3.14159; // this is a constant, it is immutable and must have a type annotation

fn main() {
    println!(
        "To make an apple {} from scratch, you must first create a universe.",
        PI
    );
}
