# Instructions for Agents

## Before Pushing

Always update `README.md` to reflect the current state of the project:
- Update the Structure section with new binaries in `src/bin/`
- Update the Running examples section with new cargo run commands
- Keep the README in sync with the actual codebase
